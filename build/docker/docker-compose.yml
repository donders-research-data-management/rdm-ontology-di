version: '3'

services:
  rdr-configurable-content:
    container_name: rdr-configurable-content
    build:
      context: ../../
      dockerfile: build/docker/Dockerfile
    image: "docker.isc.ru.nl/rdr/web/rdr-configurable-content-donders:${TAG}"
    ports:
      - 8082:80
    logging:
      options:
       max-size: "20m"
       max-file: "2"
    restart: unless-stopped
    networks:
      - rdr-network
networks:
  rdr-network:
    external: true
